{
  "name": "propwash-operator",
  "version": "0.1.0",
  "private": true,
  "description": "PROPWASH operator app — per-zone clean execution + verification (Part 107, operator in command)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0"
  }
}
